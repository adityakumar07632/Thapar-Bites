/**
 * Ops-dashboard helpers — cross-product primitives come from the shared
 * design system so both apps format money and time identically.
 */
export { cn, formatINR, formatCountdown, timeAgo } from '@campus-bites/ui';
